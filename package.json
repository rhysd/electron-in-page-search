{
  "name": "electron-in-page-search",
  "version": "0.0.1",
  "description": "Electron module to provide in-page search feature",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc -p src/",
    "lint": "tslint $(git ls-files | grep -E \"\\.ts$\")",
    "example": "cd example/browser-window && npm start",
    "watch": "guard --watchdir src",
    "preversion": "npm run lint && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rhysd/electron-in-page-search.git"
  },
  "keywords": [
    "electron",
    "module",
    "in-page",
    "search"
  ],
  "author": "rhysd <lin90162@yahoo.co.jp>",
  "license": "MIT",
  "devDependencies": {
    "@types/electron": "^1.4.26",
    "tslint": "^3.15.1",
    "typescript": "^2.0.8"
  }
}
